# Third-party notices

The SCRFD distance decoding and 106-point landmark inference in `onnx_landmarks.py` are adapted from the [InsightFace project](https://github.com/deepinsight/insightface), which declares its project code under the MIT License. Credit belongs to the InsightFace authors and contributors; the MSCH project license does not replace upstream attribution.

Optional InsightFace model weights are separate artifacts and are not included in this repository. The upstream project describes its provided pretrained models as restricted to non-commercial research; check the license for the exact weights you obtain. Do not assume this repository's MIT code license grants rights to those models.

OpenCV, NumPy, PyTorch, Pillow and optional ONNX Runtime or MediaPipe are installed separately and keep their own licenses.
